import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  statusList: {
    padding: 16,
    gap: 12,
    flexDirection: 'row',
    flexWrap: 'wrap',
    alignItems: 'center',
  },
  statusOption: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 6,
    marginRight: 20,
  },
  statusRadioIcon: {
    marginRight: 8,
  },
});
