import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    padding: 20,
    gap: 10,
    height: 240,
  },
  priceQuantityContainer: {
    flexDirection: 'row',
    gap: 10,
  },
});
