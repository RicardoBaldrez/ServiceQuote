import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'flex-end',
    gap: 4,
  },
  currency: {
    fontSize: 12,
    color: '#0F0F0F',
    marginBottom: 1,
  },
  amount: {
    fontSize: 16,
    fontWeight: 'bold',
    color: '#0F0F0F',
  },
});
